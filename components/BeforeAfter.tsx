'use client';import {useState} from 'react';export function BeforeAfter({before,after}:{before:string;after:string}){const[value,setValue]=useState(50);return <div className="compare-wrap"><div className="compare-stage"><img src={before} alt="Prima" className="compare-image"/><div className="after-mask" style={{clipPath:`inset(0 0 0 ${value}%)`}}><img src={after} alt="Dopo" className="compare-image"/></div><div className="compare-line" style={{left:`${value}%`}}/><span className="badge before-badge">PRIMA</span><span className="badge after-badge">DOPO</span></div><input className="range" type="range" min="0" max="100" value={value} onChange={e=>setValue(Number(e.target.value))}/></div>}
